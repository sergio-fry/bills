<div class="dropdown">
  <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Dropdown button
  </button>
  <ul class="dropdown-menu">
    <li><a class="dropdown-item" href="#">Action</a></li>
    <li><a class="dropdown-item" href="#">Another action</a></li>
    <li><a class="dropdown-item" href="#">Something else here</a></li>
  </ul>
</div>

<!-- As a heading -->
<nav class="navbar bg-body-tertiary">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1">Navbar</span>

    <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Dropdown link
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Action</a></li>
            <li><a class="dropdown-item" href="#">Another action</a></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
  </div>
</nav>


<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
  <!-- Container wrapper -->
  <div class="container-fluid">
    <!-- Toggle button -->
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>

    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Navbar brand -->
      <%= link_to 'Bills', root_url, class: 'navbar-brand mt-2 mt-lg-0' %>
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if @organization.present? && @organization.persisted? %>
          <li class="nav-item">
            <%= link_to @organization.name, organization_url(@organization), class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t("transactions").capitalize, organization_transactions_path(@organization), class: 'nav-link' %>
          </li>

          <li class="nav-item">
            <%= link_to t("members").capitalize, organization_memberships_path(@organization), class: 'nav-link' %>
          </li>
          <% if signed_in? %>
            <li class="nav-item">
              <%= link_to t('signout').capitalize, destroy_user_session_path, "data-method": :post, class: 'nav-link' %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <!-- Left links -->
    </div>
    <!-- Collapsible wrapper -->

  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->


